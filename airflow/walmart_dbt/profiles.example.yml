walmart_dbt:
  target: dev
  outputs:
    dev:
      type: databricks
      catalog: walmart
      host: XXXX
      http_path: XXX
      schema: dbt_schema
      threads: 1
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
